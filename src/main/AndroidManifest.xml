<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.grad.donation">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Donation">
        <activity android:name=".UserHome.Contact"></activity>
        <activity android:name=".UserHome.UserHome" />
        <activity android:name=".AdminHome.AdminHome" />
        <activity android:name=".Auth.Login" />
        <activity android:name=".Templates.DetailsTemp" />
        <activity android:name=".AdminHome.Post.See_Full_Image" />
        <activity android:name=".Auth.Signup" />
        <activity android:name=".Auth.EditProfile" />
        <activity android:name=".AdminHome.Post.AddPost" />
        <activity android:name=".Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>